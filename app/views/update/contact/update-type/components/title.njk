<h2 class="title">Change
  {% if data.contactType == "typeTalk" %}
    their {{ data.contactTypes[data.contactType].display  }}
  {% elif data.contactType == "otherContact" %}
    a contact method
  {% else %}
    their {{ data.contactTypes[data.contactType].display | lower  }}
  {% endif %}
    {{ data.contactTypes[data.contactType].type | contactIs  }}
</h2>
